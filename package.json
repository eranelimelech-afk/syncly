{
  "name": "desk-prompt-terminal",
  "version": "1.0.0",
  "description": "DESK — a Hebrew RTL trading prompt builder with a server-side LLM run route.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0"
  }
}
